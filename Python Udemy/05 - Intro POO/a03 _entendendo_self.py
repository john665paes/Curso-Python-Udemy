# Essa saula serviu apenas exeplificar o self
# o self serve para identificar o objeto que estou trabalhando
#  a classe é o molde
# A instancia é o objeto que foi criado a partir da classe
# Uma classe pode gerar várias instâncias, ou seja, objetos.
# Na classe o self representa o objeto que estou trabalhando, é a própria instância