# Operador Lógico
# and (e) or(ou) not(não)
# Usado para inverter expressões
# not True = False
# not False = True

# EX:

print(not 0) #de negativo para positivo 
print(not 1) #de positivo para negativo

# verificar se não foi digitado nada
senha = input('Senha: ')

if not senha:
    print('Senha incorreta')
